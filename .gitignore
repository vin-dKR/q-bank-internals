# Repo-level only — each app (api/, web/) has its own .gitignore for its build/deps.
node_modules/
dist/
build/
*.tsbuildinfo

.env
.env.*
!.env.example

# os / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

# logs
*.log
logs/

# Claude Code local worktrees / scratch
.claude/
